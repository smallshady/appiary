machine:
  node:
    version: "6"
dependencies:
  override:
    - "npm -g install npm@latest"
    - "npm install --no-optional"
    - "npm install -g dredd@latest"
test:
  pre:
    - "npm info dredd version"
  post:
    - dredd
